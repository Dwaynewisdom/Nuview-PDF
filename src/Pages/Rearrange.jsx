import React from 'react'
import { PageSizes } from 'pdf-lib'

function Rearrange() {
    const newPage1 = pdfDocEncodingDecode.addpage(PageSizes.A7)
    const newPage2 = pdfDocEncodingDecode.addPage(PageSizes.letter)
    const newPage3 = pdfDoc
  return (
    <div>Rearrange</div>
  )
}

export default Rearrange